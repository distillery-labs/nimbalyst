<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for Electron and V8 -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Allow spawning subprocess (for Claude Code MCP) -->
    <key>com.apple.security.inherit</key>
    <true/>
    <!-- File access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Network access -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- Microphone access for Voice Mode (local-build override) -->
    <!-- Upstream omits this because notarized builds inherit it into spawned -->
    <!-- subprocesses. In our local build that risk is acceptable, and without it -->
    <!-- macOS never shows the permission prompt and the app never appears in -->
    <!-- System Settings > Privacy & Security > Microphone. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Photos library access (osxphotos and other Apple Photos tools) -->
    <key>com.apple.security.personal-information.photos-library</key>
    <true/>
</dict>
</plist>