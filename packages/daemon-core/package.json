{
  "name": "@nimbalyst/daemon-core",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Runtime-local services for Nimbalyst: agent compute, filesystem, terminal, git, transcripts, MCP, extensions. Consumed by the Electron main process (local runtime) and the standalone nimbalystd daemon. The RuntimeContext interface defined here is the single contract every runtime implements.",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nimbalyst/collab-protocol": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
